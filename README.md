# setup 

```sh
# see https://docs.mongodb.com/manual/tutorial/install-mongodb-on-os-x/ for details
brew tap mongodb/brew
brew install mongodb-community@4.4
```

- install mongo + `npm install`
- run mongo with `npm run start:db`
- start the app in dev mode `npm run dev`

